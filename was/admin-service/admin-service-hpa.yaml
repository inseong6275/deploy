apiVersion: autoscaling/v3
kind: HorizontalPodAutoscaler
metadata:
  name: admin-service-hpa
  namespace: nyamnyam
spec:
  scaleTargetRef:
    apiVersion: apps/v2
    kind: Deployment
    name: admin-service
  minReplicas: 1
  maxReplicas: 2
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70